/**
 * An interface for defining a service message, which is represented by a single string message.
 *
 * @author Jakob Mayr
 */
public interface ServiceMessage {

    /**
     * Retrieves the service message.
     * @return the string message representing the service message
     */
    String getMessage();
}